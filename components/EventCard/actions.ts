"use server";

export async function duplicate(id: string) {
  console.log(`Duplicating event with ID ${id}`);
  // TODO: Implement event duplication and revalidation
}

export async function archive(id: string) {
  console.log(`Archiving event with ID ${id}`);

  // TODO: Implement event archiving and revalidation
}
